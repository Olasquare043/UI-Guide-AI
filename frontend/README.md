# UI Guide Frontend

React + Vite client for the UI Guide assistant.

## Setup

```
cd frontend
npm install
cp .env.example .env
npm run dev
```

## Scripts

- `npm run build`
- `npm run lint`
- `npm run format`
- `npm test`
